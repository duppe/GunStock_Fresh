// Import the Vue framework
import Vue from 'vue';

// Initialize the Vue when the document has loaded
document.addEventListener('DOMContentLoaded', function (event) {
    let view = new Vue({
        el: document.getElementById('view'),
        components: {
        },
        data: {
            errorMessages:''
        },

        methods: {
        }
    });
});